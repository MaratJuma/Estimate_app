# Проект смет

Цель: сделать веб-приложение для хранения услуг, подрядчиков и формирования смет.

Стек:
- Python
- Django
- PostgreSQL

## День 1

Сделано:
- установлен Python
- установлен VS Code
- установлен Git
- создан первый Python-скрипт
- реализован расчёт сметы по услугам

Понял:
- список хранит набор услуг
- словарь хранит поля одной услуги
- цикл for перебирает услуги
- можно считать общую сумму, маржу и процент маржи

## День 2

Сделано:
- создана база estimate_db
- созданы таблицы contractors, services, estimates, estimate_items
- вставлены тестовые данные
- выполнены SELECT и JOIN запросы

Понял:
- как таблицы связаны между собой
- что смета состоит из заголовка и позиций
- что JOIN позволяет получать данные из связанных таблиц

## День 3

Сделано:
- создано виртуальное окружение
- установлен Django
- создан проект config
- создано приложение core
- проект успешно запущен в браузере
- создан первый маршрут и первая страница

Понял:
- manage.py запускает команды Django
- settings.py хранит настройки проекта
- urls.py отвечает за маршруты
- views.py отвечает за логику страницы
- приложение core подключается через INSTALLED_APPS

## День 4

Сделано:
- установлен psycopg2-binary
- создана база estimate_django_db
- Django подключён к PostgreSQL
- созданы модели Contractor, Service, Estimate, EstimateItem
- выполнены makemigrations и migrate

Понял:
- модель Django описывает таблицу БД
- ForeignKey задаёт связи между таблицами
- makemigrations создаёт описание изменений
- migrate применяет изменения к базе

## День 5

Сделано:
- создан суперпользователь
- открыта админка Django
- модели зарегистрированы в admin.py
- добавлены подрядчики, услуги, сметы и позиции смет через браузер
- настроены list_display, search_fields, list_filter

Понял:
- админка Django — это готовый внутренний интерфейс
- через admin можно быстро наполнять базу
- записи, созданные в админке, сохраняются в PostgreSQL

## День 6

Сделано:
- создано представление service_list
- создан маршрут /services/
- созданы шаблоны home.html и service_list.html
- список услуг выводится из PostgreSQL через Django
- выводятся связанные данные подрядчика

Понял:
- view получает данные из модели
- template отображает данные в HTML
- urls.py связывает адрес страницы и view

## День 7

Сделано:
- добавлен поиск по услугам
- используется GET-параметр q
- список услуг фильтруется по названию
- на страницу передаётся query и count
- добавлен вывод "ничего не найдено"

Понял:
- request.GET получает параметры из URL
- форма method="get" отправляет данные в адрес страницы
- Django ORM может фильтровать данные по подстроке через icontains

## День 8

Сделано:
- создана страница списка смет /estimates/
- создана страница одной сметы /estimates/<id>/
- выводятся позиции сметы
- добавлены ссылки между главной, услугами и сметами

Понял:
- get_object_or_404 получает объект по id или возвращает 404
- связанные позиции можно получить через estimate.items.all()
- Django удобно строит страницы списка и деталей

## День 9

Сделано:
- создан файл forms.py
- создана форма EstimateForm
- добавлено представление estimate_create
- создан маршрут /estimates/create/
- сделан шаблон формы
- новая смета сохраняется в БД через пользовательский интерфейс
- после сохранения происходит переход на страницу сметы

Понял:
- ModelForm строит форму на основе модели
- GET показывает форму
- POST отправляет данные формы
- form.is_valid() валидирует данные
- form.save() сохраняет объект в БД
- redirect переводит пользователя на другую страницу

## День 10

Сделано:
- создана форма EstimateItemForm
- реализовано добавление позиции в смету
- сделан маршрут /estimates/<id>/items/add/
- позиция привязывается к конкретной смете
- cost_price и client_price берутся из услуги
- total_cost и total_client рассчитываются автоматически

Понял:
- form.save(commit=False) позволяет изменить объект перед сохранением
- позицию можно программно связать со сметой
- бизнес-логика расчёта может жить во view

## День 11

Сделано:
- на странице сметы посчитаны итоговые суммы
- выводится общая себестоимость
- выводится общая сумма для клиента
- выводится маржа

Понял:
- итоговые значения можно считать по связанным позициям
- данные можно сначала собрать из модели, а потом обработать во view
- страница сметы стала полноценнее и полезнее

## День 12

Сделано:
- создан базовый шаблон base.html
- страницы переведены на наследование шаблонов
- общая навигация и стили вынесены в одно место
- интерфейс стал единообразным

Понял:
- {% extends %} подключает базовый шаблон
- {% block %} позволяет вставлять содержимое в базовый шаблон
- общий layout упрощает поддержку проекта

## День 13

Сделано:
- шаблоны переведены на {% url %}
- redirect() переведён на именованные маршруты
- убраны жёстко прописанные ссылки в основных шаблонах

Понял:
- name в urls.py задаёт имя маршрута
- {% url %} строит ссылку по имени маршрута
- redirect() тоже может работать по имени маршрута
- это делает проект более устойчивым к изменениям

## День 14

Сделано:
- проверен полный пользовательский сценарий
- улучшено отображение дат
- улучшены подписи на страницах
- приведены в порядок views и шаблоны
- составлен список следующих улучшений

Понял:
- проект уже работает как минимальный MVP
- базовая структура Django-приложения собрана
- можно дальше улучшать удобство интерфейса и бизнес-логику

## День 15

Сделано:
- добавлено удаление позиции сметы
- создана страница подтверждения удаления
- после удаления происходит возврат к смете
- итоги сметы пересчитываются автоматически

Понял:
- удаление объекта должно выполняться через POST
- get_object_or_404 удобно использовать для получения позиции по id
- после удаления можно сделать redirect на связанную сущность

## День 16

Сделано:
- добавлено редактирование позиции сметы
- реализована форма редактирования с instance=item
- при сохранении пересчитываются цены и totals
- на странице сметы появились кнопки редактирования

Понял:
- instance=... позволяет редактировать существующий объект через ModelForm
- одну и ту же форму и шаблон можно переиспользовать для create и update
- производные поля нужно пересчитывать при каждом изменении исходных данных

## День 17

Сделано:
- добавлено редактирование сметы
- форма редактирования работает через instance=estimate
- шаблон формы сметы переиспользуется для create и update
- на странице сметы появилась кнопка редактирования

Понял:
- одну и ту же ModelForm удобно использовать и для создания, и для редактирования
- instance=... позволяет менять существующий объект
- шаблон можно делать универсальным через is_edit

## День 18

Сделано:
- добавлен поиск услуг в форме добавления позиции
- реализована фильтрация по name через GET-параметр q
- queryset поля service ограничивается найденными услугами
- форма добавления позиции стала удобнее при большом количестве услуг

Понял:
- queryset поля формы можно менять во view
- GET удобно использовать для поисковых и фильтрующих форм
- одна страница может содержать и форму поиска, и форму сохранения данных

## День 19

Сделано:
- добавлен фильтр услуг по категории в форме добавления позиции
- категории подгружаются из базы
- фильтрация работает отдельно и вместе с поиском по названию
- выбранные фильтры сохраняются при отправке формы

Понял:
- queryset можно фильтровать сразу по нескольким условиям
- values_list(...).distinct() удобно использовать для получения списка значений
- hidden input помогает сохранять состояние фильтра между запросами

## День 20

Сделано:
- добавлена модель EstimateDay
- настроена связь Estimate -> EstimateDay
- для новой сметы автоматически создаётся День 1
- модель EstimateDay зарегистрирована в админке

Понял:
- многодневную смету лучше строить через отдельную сущность дня
- архитектуру лучше менять поэтапно, не ломая весь проект сразу
- unique_together помогает защитить структуру данных

## День 21

Сделано:
- добавлено создание нового дня в смете
- следующий номер дня определяется автоматически
- на странице сметы появилась кнопка "Добавить день"
- список дней отображается на странице сметы

Понял:
- создание новой связанной сущности лучше выполнять через POST
- номер следующего дня можно вычислять на основе последнего существующего дня
- многодневная структура сметы собирается постепенно и безопасно

## День 22

Сделано:
- EstimateItem переведён на связь с EstimateDay
- добавление позиции теперь происходит для конкретного дня
- на странице сметы позиции группируются по дням
- считаются итоги по каждому дню и общий итог по смете

Понял:
- правильная многодневная структура требует связи позиции с конкретным днём
- вложенный вывод в шаблоне помогает отображать иерархию данных
- общие итоги можно собирать через промежуточные итоги по дням

## День 23

Сделано:
- добавлено удаление дня из сметы
- создана страница подтверждения удаления дня
- показывается количество позиций в удаляемом дне
- при удалении дня автоматически удаляются и его позиции
- после удаления выполняется возврат на страницу сметы

Понял:
- on_delete=CASCADE позволяет удалять связанные позиции автоматически
- удаление структуры данных лучше сопровождать явным подтверждением
- при удалении связанной сущности важно заранее сохранить id родительского объекта для redirect

## День 25

Сделано:
- добавлена отдельная форма создания поставщика
- добавлена форма создания услуги
- в форме услуги реализован выбор поставщика из списка
- добавлен поиск поставщика по названию
- добавлен фильтр поставщика по категории
- из формы услуги можно перейти к созданию нового поставщика
- после создания поставщика происходит возврат в форму добавления услуги

Понял:
- отдельные CRUD-формы проще и чище, чем комбинированная форма с несколькими сценариями
- queryset поля формы можно динамически фильтровать во view
- параметр next помогает вернуть пользователя в нужный сценарий после промежуточного действия
## Дополнение к Дню 25

Сделано:
- в модель Contractor добавлены поля contact_name, phone, email
- форма добавления поставщика обновлена
- данные контакта теперь можно сохранять вместе с поставщиком

Понял:
- контактные данные поставщика лучше хранить прямо в сущности Contractor
- телефон нужно хранить как строку, а email — как EmailField
- новые поля безопаснее добавлять как необязательные, чтобы не ломать существующие данные

## День 26

Сделано:
- форма создания позиции сметы оставлена простой: услуга + количество
- форма редактирования позиции расширена полями cost_price и client_price
- при редактировании totals пересчитываются по введённым вручную ценам
- цены позиции больше не перезаписываются автоматически из таблицы услуг при update

Понял:
- цена в смете должна храниться как независимое значение, а не всегда зависеть от справочника услуг
- для разных сценариев удобно использовать разные формы create/update
- расчёт total должен опираться на значения самой позиции сметы
## Дополнение к Дню 26
Сделано:
- в форме редактирования позиции убрано редактируемое поле услуги
- название услуги теперь отображается как текст
- редактируются только количество и цены

Понял:
- изменение услуги в уже созданной позиции делает поведение формы неоднозначным
- в редактировании лучше фиксировать услугу, а менять только параметры строки
- если нужна другая услуга, логичнее создать новую позицию
## Корректировка Дня 26
Сделано:
- в форме редактирования позиции сметы убрано редактирование себестоимости
- редактировать можно только количество и цену клиенту
- себестоимость показывается как текстовое значение

Понял:
- не все финансовые параметры обязательно должны быть доступны для редактирования
- можно разделять отображаемые и редактируемые поля в одной форме
- total_cost должен пересчитываться по сохранённой себестоимости и новому количеству

## День 27

Сделано:
- список смет переведён в табличный формат
- добавлен поиск по клиенту, менеджеру и комментарию
- для каждой сметы в списке считаются количество дней, себестоимость, сумма клиенту и маржа
- в таблицу добавлены действия: открыть, редактировать, дублировать

Понял:
- для рабочих списков таблица удобнее карточек
- поиск лучше делать по нескольким полям, если это не усложняет интерфейс
- агрегаты по связанным данным удобно рассчитывать во view и передавать в шаблон уже готовыми

## День 28

Сделано:
- список услуг переведён в табличный формат
- создана отдельная страница списка поставщиков
- список поставщиков тоже отображается таблицей
- добавлена навигация между услугами и поставщиками

Понял:
- справочники удобнее просматривать в таблицах, чем в карточках
- для связанных данных полезно использовать select_related
- лучше сначала сделать читаемые списки, а затем добавлять в них действия CRUD
## Дополнение к Дню 28

Сделано:
- в список услуг возвращён поиск
- добавлен поиск по названию услуги
- добавлен фильтр по категории поставщика
- список услуг остаётся в табличном формате

Понял:
- при рефакторинге интерфейса важно следить, чтобы не терялся уже реализованный полезный функционал
- фильтрация услуг по категории может идти через связанного поставщика
- для списков категорий лучше исключать пустые значения, если поле category необязательное
## Дополнение к Дню 28

Сделано:
- в список поставщиков добавлены поиск по названию и фильтр по категории
- подтверждено бизнес-правило: поставщики не удаляются, только редактируются

Понял:
- поставщик является опорной сущностью справочника, поэтому его удаление лучше не предусматривать
- интерфейс списков поставщиков и услуг должен быть симметричным: таблица, поиск, фильтр

## День 29

Сделано:
- добавлено редактирование поставщиков
- добавлено редактирование услуг
- добавлено удаление услуг через отдельную страницу подтверждения
- в таблицах услуг и поставщиков появилась колонка действий

Понял:
- для create/update удобно использовать один и тот же шаблон формы
- удаление лучше делать через отдельный экран подтверждения
- удаление справочников нужно проверять с учётом связанных сущностей и on_delete

## Дополнение к Дню 29

Сделано:
- добавлена защита удаления услуги, если она уже используется в сметах
- перед удалением выполняется проверка наличия связанных EstimateItem
- при запрете удаления пользователю показывается понятное сообщение

Понял:
- удаление справочников нужно защищать, если они уже участвуют в бизнес-документах
- лучше сочетать защиту на уровне view и на уровне модели через on_delete=PROTECT
- интерфейс должен не просто запрещать действие, но и объяснять пользователю причину

## Следующий шаг после Дня 29

Сделано:
- список поставщиков отсортирован по id по убыванию, новые сверху
- в модель EstimateDay добавлены title и description
- реализовано редактирование названия и описания дня
- на странице сметы теперь отображаются название и описание дня

Понял:
- дни сметы полезно делать не просто нумерованными блоками, а содержательными этапами
- для создания и редактирования дня лучше использовать разные формы
- сортировка справочника по убыванию id удобна, когда важны последние добавленные записи

## Дополнение по дням

Сделано:
- при создании дня номер больше не вводится вручную
- day_number назначается автоматически как следующий после максимального существующего номера
- форма создания дня упрощена: остались только название и описание

Понял:
- автоматическая нумерация уменьшает количество ошибок пользователя
- для последовательной нумерации лучше использовать max(day_number) + 1, а не count() + 1
- порядок дней полезно фиксировать через Meta.ordering

## Дополнение по удалению дней

Сделано:
- удаление дня запрещено, если в нём есть позиции
- при удалении пустого дня все последующие дни в этой смете перенумеровываются на -1

Понял:
- структура дней должна оставаться последовательной после удаления
- перенумерацию удобнее делать через update с F-выражением
- удаление структурного элемента нужно защищать, если внутри него есть данные

## Печатная / клиентская версия сметы

Сделано:
- добавлена отдельная клиентская версия сметы
- в ней скрыты служебные кнопки и внутренние финансовые показатели
- клиентская версия показывает только дни, услуги, количество, цену клиенту и итог
- добавлена кнопка печати / сохранения в PDF через браузер

Понял:
- для клиента нужно выводить только клиентские цены, без себестоимости и маржи
- печатную версию лучше делать отдельным шаблоном, а не пытаться скрыть части рабочего интерфейса
- браузерная печать — быстрый способ получить PDF на этапе MVP

## Улучшение клиентской версии сметы

Сделано:
- клиентская версия сметы приведена к более коммерческому виду
- таблица услуг получила фиксированную структуру колонок
- добавлены шапка документа, реквизиты компании, дата, номер сметы, блок клиента и подпись
- в документе оставлены только клиентские цены и итоговая стоимость
- добавлен фильтр форматирования денежных значений
- пустые дни можно исключать из клиентской версии

Понял:
- клиентский документ должен отличаться от рабочего интерфейса не только отсутствием кнопок, но и общей структурой
- для печатных таблиц важно использовать фиксированные ширины колонок
- форматирование денег сильно влияет на коммерческое восприятие документа

## Утверждение сметы

Сделано:
- в модель Estimate добавлены признаки утверждения: is_approved и approved_at
- реализована кнопка и отдельный сценарий утверждения сметы
- после утверждения смета блокируется от редактирования
- редактирующие действия запрещены как на уровне интерфейса, так и на уровне views

Понял:
- утверждённая смета должна работать как зафиксированный документ
- данные сметы должны использовать сохранённые значения EstimateItem, а не текущие значения из справочника услуг
- блокировку редактирования нужно делать не только в шаблонах, но и в серверной логике

## Динамическая себестоимость в форме редактирования позиции

Сделано:
- в форме редактирования позиции сметы отображается актуальная себестоимость из карточки услуги
- при сохранении позиции total_cost пересчитывается по текущей себестоимости услуги
- в неутверждённой смете форма редактирования и страница сметы теперь согласованы по логике себестоимости

Понял:
- если черновик сметы использует динамическую себестоимость, это должно одинаково отражаться и в деталке, и в форме редактирования
- отображаемые значения и сохраняемые значения в черновике могут различаться, поэтому лучше поддерживать их синхронизацию при сохранении

## Синхронизация себестоимости в черновиках смет

Сделано:
- при изменении себестоимости услуги обновляются все связанные позиции в неутверждённых сметах
- в этих позициях пересчитываются cost_price и total_cost
- утверждённые сметы остаются неизменными

Понял:
- черновик сметы должен автоматически следовать за актуальной себестоимостью услуги
- синхронизацию удобно выполнять в момент сохранения услуги
- для архивной целостности утверждённые сметы нужно полностью исключать из массового обновления

## Согласование расчётов списка смет

Сделано:
- логика расчётов в estimate_list приведена в соответствие с логикой черновика и утверждённой сметы
- для неутверждённых смет себестоимость берётся из актуальной карточки услуги
- для утверждённых смет расчёты идут по зафиксированным ценам позиции сметы
- стоимость для клиента и маржа в списке смет теперь совпадают с деталкой сметы

Понял:
- список смет должен считать показатели по той же бизнес-логике, что и сама смета
- безопаснее рассчитывать totals из qty и unit price, чем безусловно доверять сохранённым total-полям
- после изменения модели поведения черновик/утверждение нужно синхронизировать все экраны системы

## Дополнение к списку смет

Сделано:
- в таблицу списка смет добавлен статус сметы
- теперь видно, какие сметы утверждены, а какие находятся в работе
- для утверждённых смет в списке скрыта ссылка на редактирование

Понял:
- список документов должен показывать не только суммы, но и жизненный статус документа
- действия в интерфейсе лучше подстраивать под текущее состояние сущности

## День 30 — авторизация и роли, этап 1

Сделано:
- подключена стандартная авторизация Django
- добавлены страницы входа и выхода
- система закрыта для неавторизованных пользователей через login_required
- реализованы роли через superuser и группы Django:
  - production_manager
  - sales_manager
- создан модуль permissions.py с централизованной логикой прав
- права применены на уровне views

Матрица доступа:
- admin: доступ ко всем разделам и действиям
- production_manager: управление поставщиками и услугами, утверждение смет
- sales_manager: создание и редактирование смет

Понял:
- права нужно обязательно проверять на сервере, а не только скрывать кнопки в интерфейсе
- стандартные User/Group Django достаточно гибкие для ролевой модели проекта
- разделение прав на просмотр, редактирование и утверждение удобнее, чем одно общее право

## День 30 — авторизация и роли, этап 2

Сделано:
- добавлен context processor с флагами прав пользователя для шаблонов
- верхнее меню теперь отображается с учётом прав
- в списке смет и в карточке сметы скрываются действия, недоступные текущей роли
- в списках услуг и поставщиков скрываются кнопки изменения для пользователей без прав
- в шапке отображается имя роли пользователя

Понял:
- серверных проверок недостаточно для хорошего UX, интерфейс тоже должен учитывать права
- context processor удобен для централизованной передачи флагов доступа во все шаблоны
- роли лучше отображать пользователю явно, особенно на этапе тестирования

## Разделение create/update формы услуги

Сделано:
- форма создания услуги сохраняет поиск и фильтрацию
- форма редактирования услуги упрощена
- при редактировании убраны поиск, выбор поставщика и изменение названия услуги
- в edit-форме отображаются только название услуги и имя поставщика в readonly-виде
- редактируемыми остаются только цены и комментарий

Понял:
- сценарии создания и редактирования услуги имеют разную бизнес-логику и должны обслуживаться разными формами
- поисковые и выборочные элементы нужны на этапе создания, но мешают и избыточны при редактировании

## Экспорт рабочей сметы в Excel

Сделано:
- от идеи отдельного PDF-экспорта отказались в пользу печати из браузера
- начата реализация экспорта рабочей сметы в Excel через openpyxl
- Excel-экспорт включает внутренние данные: себестоимость, цену клиенту, суммы и маржу
- добавлены итоги по дням и общий итог по смете
- экспорт оформлен как скачивание .xlsx файла

Понял:
- для клиентской версии на текущем этапе достаточно браузерной печати в PDF
- Excel лучше подходит для внутренней рабочей сметы, чем PDF
<!-- - рабочая смета содержит чувствительные данные, поэтому права на её экспорт нужно ограничивать отдельно -->